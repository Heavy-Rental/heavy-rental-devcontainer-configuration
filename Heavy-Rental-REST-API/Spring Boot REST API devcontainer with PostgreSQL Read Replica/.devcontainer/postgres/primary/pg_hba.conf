# Allow local connections
local   all             all                                     trust
# Allow app and replica connections
host    all             all             0.0.0.0/0               md5
# Allow replica connection from standby
host    replication     replicator      0.0.0.0/0               md5
