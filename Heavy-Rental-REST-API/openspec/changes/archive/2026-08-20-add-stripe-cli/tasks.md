# Tasks: Add Stripe CLI

## 1. Images

- [x] 1.1 Install `stripe` via official Debian repo in with-replica Dockerfile
- [x] 1.2 Same install in without-replica Dockerfile
- [x] 1.3 Add `start-stripe-listen.sh` to both packs and COPY into the image
- [x] 1.4 Wire `postStartCommand` to the helper in both `devcontainer.json` files

## 2. Specs and docs

- [x] 2.1 OpenSpec delta + SoT + this archive
- [x] 2.2 Spec Kit FR/SC/verification/quickstart
- [x] 2.3 Pack README + OpenSpec README + OpenSPDD canvas
